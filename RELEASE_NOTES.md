# Release Notes for v0.1.0

Initial release — DisplayCalibration. How to run: use §2 in README (run capture-app server to capture calibration_result.json). Includes C++ demo and build instructions. See README.md for details.